<div class="row">
  <div class="col-lg-12 col-md-12">
    <div class="inici-sessio-container">
      <p>Següent taller:</p>
      <p class="sala-espera-text"><%= @estat.anunci %></p>

      <br/>
      <br/>
      <div class="sala-espera-entrar">
        <% if @estat.obert == false %>
          <p>Encara no es pot entrar</p>
        <% else %>
          <%= link_to "Entrar", home_index_path, class: 'btn btn-aventura' %>
        <% end %>
      </div>
      <br/>
      <br/>
    </div>
  </div>
</div>